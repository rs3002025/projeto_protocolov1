# Variáveis de ambiente para o banco de dados
# Copie este arquivo para .env e preencha com os valores corretos.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Chave secreta para assinar os JSON Web Tokens (JWT)
# Use um valor longo, aleatório e seguro em produção
JWT_SECRET="SEGREDO_SUPER_SECRETO_PARA_PRODUCAO"

# Variáveis do Supabase (para o client-side ou outras integrações)
# Estas já estavam sendo carregadas via process.env, então vamos mantê-las aqui para centralização.
SUPABASE_URL="URL_DO_SEU_PROJETO_SUPABASE"
SUPABASE_SERVICE_KEY="SUA_CHAVE_DE_SERVICO_SUPABASE"
